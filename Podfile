xcodeproj 'Realmbox.xcodeproj'
platform :ios, '10.0'
use_frameworks!

target 'Realmbox' do
platform :ios, '9.3'
    pod 'ObjectiveDropboxOfficial'
    pod 'Realm'

    target "RealmboxTests" do
        inherit! :search_paths
    	pod 'ObjectiveDropboxOfficial'
    	pod 'Realm'
    end

end
